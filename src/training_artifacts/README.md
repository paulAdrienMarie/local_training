# Repository for the generated artifacts

To generate the training artifacts, run : `python artifacts.py`, then will appear in this folder the following files:
  - training_model.onnx
  - eval_model.onnx
  - checkpoint
  - optimizer_model.onnx
